# 京域智城 × 灵境哨兵比赛演示验收合同

本合同是每次修改、打包和部署前必须通过的固定流程。任何视觉优化不得破坏下列顺序或能力。

## 固定用户路径

1. 打开项目后显示京域智城登录页，不允许长期停留在“安全环境初始化中”。
2. 使用 `admin / 123456` 登录。
3. 登录后首先进入全新的“北京智慧城市 3D 大屏”，不得直接进入应急项目。
4. 智慧城市大屏中的灵境哨兵入口只能有一个，位于右下角。
5. 点击右下角入口后进入灵境哨兵，中央真实北京 3D 地图必须可见。
6. 地图必须支持旋转、缩放、昼夜、楼宇灯光、车辆跟随和道路交互。
7. 点击“一键完整演示”后，72 秒状态机必须能够暂停、继续、跳转阶段和倍速播放。
8. 上传 JPG、PNG 或 WebP 图片后，必须在浏览器本机运行 ONNX 模型并显示目标框、类别、置信度与耗时。
9. 模型失败时必须明确显示“可解释降级”，不得把像素阈值结果冒充深度学习输出。
10. “AI 证据链”必须展示模型来源、输入输出、指标、混淆矩阵、测试样本和误判案例。
11. “协同审计”必须展示任务输入、智能体消息、决策依据、执行状态，并能导出 JSON。
12. 风险扩散必须标注为高斯烟羽物理模型，路径重规划必须标注为 OSM A* 算法。
13. 封路后必须产生新的道路路线、距离、ETA 与搜索节点数。
14. 所有数据卡必须打开清晰、可关闭的详情弹窗。
15. 应急处置报告必须包含视觉结果、扩散区、真实道路路线和联合处置指令，并可下载。

## 发布与离线合同

- `npm test` 必须通过当前产品合同、生产渲染、ONNX 小型验证集和离线资产检查。
- `offline-demo` 必须包含 ONNX 模型、WASM 运行时、地图、建筑数据和验证样本。
- 智慧城市大屏必须加载本地北京 OSM 城市底图；日间与夜间模式下道路、建筑和数据面板都必须清晰可读。
- 建筑实景必须按建筑编号、最近道路和 WGS84 坐标独立查询；本地照片必须完成编号核验，KartaView 结果必须标注为“附近街景候选、待人工核验”。无覆盖或断网时明确显示缺图，禁止复用其他建筑照片。
- 交付包必须包含 `runtime/node.exe` 便携运行环境；`启动演示.bat` 必须优先使用该环境启动本地离线版本。
- 启动失败时命令窗口必须保留错误信息，不得一闪而过；默认端口被占用时必须自动尝试后续端口。
- 本地服务器必须正确返回 `.mjs`、`.wasm` 和 `.webp` MIME 类型。
- 压缩包、GitHub、GitHub Pages 和生产站点必须来自同一个已验证提交。

## 能力边界

- AI：YOLO-26n 火焰/烟雾 ONNX 目标检测。
- 物理模型：Gaussian Plume / Pasquill-D。
- 搜索算法：OSM 道路图上的 A*。
- 多智能体：确定性、可审计的软件任务编排，不宣称为大语言模型自主决策。
- 所有演示结论必须由真实指挥员复核，不作为正式应急行动依据。
