# KartaView 建筑街景方案（已停止用于建筑本体匹配）

智慧城市大屏不依赖百度地图接口或浏览器端 AK。早期版本会按建筑附近道路坐标查询 KartaView，并以 Wikimedia Commons 名称搜索作为回退。全量审查证明这种候选链路无法确保照片主体就是目标楼宇，因此已经从建筑本体主图逻辑中移除。

## 停用原因

1. 道路街景证明的是道路环境，不是建筑本体。
2. 多栋建筑可能共享同一个道路查询点。
3. 名称搜索可能返回其他城市、规划文件、区域天际线或同名对象。
4. 动态候选没有固定 `buildingId → photoId` 映射，无法重复验收。

## 当前严格流程

1. 点击建筑后先按 `buildingId` 查询 `verifiedBuildingPhotos`。
2. 只有显式登记、来源明确并完成主体核验的照片才显示。
3. 未命中时返回 `provider: "unmatched"`。
4. 未匹配建筑显示“暂无已核验本体照片”。
5. 区域图、道路街景和同名候选不再进入建筑本体主图位。

若比赛要求更多建筑拥有现实照片，应继续扩充 `public/building-photos`，并在 `verifiedBuildingPhotos` 中逐栋登记资产编号、作者、许可、来源和核验证据。没有证据时保持缺图，不自动补图。

官方说明：<https://kartaview.org/doc/faq>
