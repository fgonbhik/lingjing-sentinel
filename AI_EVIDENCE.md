# AI 证据说明

## 本地模型

- 名称：YOLO-26n Fire & Smoke
- 格式：ONNX FP32
- 输入：`1 × 3 × 640 × 640`
- 输出：`1 × 300 × 6`
- 类别：Smoke、Fire
- 运行方式：ONNX Runtime Web / WASM
- 隐私：图片仅在浏览器本地处理，不上传服务器
- 模型来源：`DarshanM0di/fireandsmoke`
- 许可：MIT

模型文件和 WASM 运行时随离线包一起分发，因此断网时仍可推理。

## 指标口径

模型作者报告：

- AP50：74.3%
- Precision：73.6%
- Recall：68.3%
- F1：约 70.8%（由 Precision 与 Recall 计算）

以上属于模型作者公开指标，不作为本项目自主复测结果。

项目内置 7 例小型烟雾测试集用于回归和现场复核。当前实测结果：

- TP：3
- FP：1
- TN：3
- FN：0
- Accuracy：85.7%
- Precision：75.0%
- Recall：100.0%
- F1：85.7%

该集合规模很小，包含项目场景资产和开放许可样本，不能代表模型在真实城市环境中的泛化能力。

## 已知误判

模型把橙色城市灯光识别为烟雾，置信度约 55.6%。因此视觉模型只作为告警触发器，正式判断必须联合气体传感器、车辆遥测和人工复核。

## 可复现验证

运行 `npm run ai:evaluate` 会执行本地 ONNX 推理，并将逐例结果与汇总指标写入 `outputs/ai-benchmark-report.json`。

运行 `npm test` 会同时验证：

- 登录和导航顺序；
- 右下角唯一入口；
- 真实 ONNX 模型与离线运行时；
- AI 证据与多智能体审计界面；
- 3D 地图、数据详情、封路重规划和报告能力；
- 离线包的核心资产与相对路径。
